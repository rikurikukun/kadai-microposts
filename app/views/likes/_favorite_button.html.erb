<% unless current_user == user %>
  <% if current_user.favoriting?(user) %>
    <%= form_for(current_user.likes.find_by(micropost_id: user.id), html: { method: :delete }) do |f| %>
      <%= hidden_field_tag :micropost_id, user.id %>
      <%= f.submit 'No like', class: 'btn btn-danger btn-block' %>
    <% end %>
  <% else %>
    <%= form_for(current_user.likes.build) do |f| %>
      <%= hidden_field_tag :micropost_id,user.id %>
      <%= f.submit 'Like', class: 'btn btn-primary btn-block' %>
    <% end %>
  <% end %>
<% end %>